# 关键决策记录

> 设计拍板、数据口径、根因与取舍。倒序（最新在上）。维护：归档员。

## 2026-09-08 BC 等级归入 C 级
- 背景：结算单品种规格出现 A/B/C/BC 四种等级标识，报表需统一分级。
- 拍板：BC 等级归入 C 级，报表显示为「C果（C/BC级）」。
- 依据：历史报表标题「C果（C/BC级）」佐证。
- 位置：`TODO.md`、`scripts/parse_settlements.py`（`rgrade` 函数）。

## 2026-09-08 销售地区字段预留为空
- 背景：销售明细暂无地区字段，但用户需求明确提到「销售地区」。
- 拍板：数据模型预留 `销售地区` 字段为空，待后续数据带上地区后回填。
- 推测：当前货都走海吉星，推测为南宁/广西，待确认。
- 位置：`data/sales_records.csv`、`TODO.md`。

## 2026-09-08 第一期范围：不做利润测算
- 背景：最终目标含利润测算，但当前没有成本数据。
- 拍板：第一期先对存量销售数据做存档与可视化报表，利润测算留第二期。
- 依据：用户明确「目前仍没有成本的数据」。

## 2026-09-08 数据录入方式：上传 Excel/CSV 导入
- 拍板：用户维护表格文件，整批导入；暂不做粘贴文本自动解析或逐条表单录入。

## 2026-09-08 运行形态：先出数据模型与报表原型
- 拍板：先用表格/静态原型确认口径，暂不写正式系统（不先复用 stock_ana 的 Vue/FastAPI 全栈）。
