# 统一修复计划

> 状态：已完成可执行修复；剩余为运维迁移与真实环境验收。
> 最后更新：2026-09-18

## 修复原则

- 先修已复现、证据明确的阻断问题。
- 涉及共享 schema、数据口径、根配置的改动先保持兼容，再补迁移或校验。
- 每批修改后运行可执行验证，不允许声称未验证通过。

## Wave 1：用户端导入/导出与确认链路

- [x] `frontend/src/views/ImportReviewView.vue`：`doSubmit` 首次 `force=false`，409 冲突/阻断后再显式 `force=true`。
- [x] `frontend/src/views/ImportView.vue`：恢复多文件选择/提交，展示预览 `failures`，不再静默丢弃文件。
- [x] `backend/app/parser/settlement_template.py`：按 CSV 后缀走 `read_csv` 分支。
- [x] 恢复/生成 `attachments/结算单模板样式.xlsx`（改为内存生成，不再依赖缺失附件），修复 4 个后端导出失败。

## Wave 2：管理端权限/配置/审计/通知

- [x] `fruits_ana_admin/backend/app/main.py`、`db.py`、`models.py`：共享业务表禁止 `create_all`。
- [x] `fruits_ana_admin/backend/app/seed.py`：种子只补缺不覆盖，移除 `test` 用户强制授权。
- [x] 控制台授权/RBAC：接入角色与权限点，`is_super_admin` 生效。
- [x] 审计日志脱敏 `password`。
- [x] 系统配置实际接入认证/注册/密码策略。
- [x] 通知目标校验、AI 缓存删除留痕；定时发布仍保留人工发布/创建即发。

## Wave 3：跨系统业务口径

- [x] 字段转换目标值只允许合法 `StandardGrade`；规则变更页面增加历史等级不重算提示。
- [x] 统一结算摘要主字段语义、售后金额符号；清关字段沿用现有费用项口径。
- [x] 覆盖导入/手工单增加 `source_type` 校验与统一审计。
- [x] 统一录单字典与用户端 `AB/BC` 口径。

## Wave 4：性能与稳定性

- [x] 分析接口默认日期窗口；结算单列表数据库分页；候选清单轻量化。
- [x] 去管理端通知/角色/用户 N+1；通知大字段改 MEDIUMTEXT 并增加 2MB 校验。
- [x] 前端请求超时/取消；用户端移动端表格卡片化；统一异常处理沿用现有客户端归一化。

## 验证

- 用户端后端全量 pytest。
- 用户端前端 test/typecheck/build。
- 管理端 typecheck/build；如补装 pytest，则运行管理端后端测试。
